import RouteLayout from '@/components/RouteLayout';
import { createPageMetadata } from '@/lib/seo';

export const metadata = createPageMetadata('/unity-game-development');

export default RouteLayout;
